{% extends 'base.html.twig' %}

{% block title %}New CartType{% endblock %}

{% block body %}
    <h1>Create new CartType</h1>

    {{ include('cart_type/_form.html.twig') }}

    <a class="btn btn-info" href="{{ path('cart_type_index') }}">back to list</a>
{% endblock %}
