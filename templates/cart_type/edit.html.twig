{% extends 'base.html.twig' %}

{% block title %}Edit CartType{% endblock %}

{% block body %}
    <h1>Edit CartType</h1>

    {{ include('cart_type/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-info" href="{{ path('cart_type_index') }}">back to list</a>

    {{ include('cart_type/_delete_form.html.twig') }}
{% endblock %}
