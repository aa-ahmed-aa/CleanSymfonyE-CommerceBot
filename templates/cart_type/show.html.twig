{% extends 'base.html.twig' %}

{% block title %}CartType{% endblock %}

{% block body %}
    <h1>CartType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cart_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ cart_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info" href="{{ path('cart_type_index') }}">back to list</a>

    <a class="btn btn-warning" href="{{ path('cart_type_edit', {'id': cart_type.id}) }}">edit</a>

    {{ include('cart_type/_delete_form.html.twig') }}
{% endblock %}
